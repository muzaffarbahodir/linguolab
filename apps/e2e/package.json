{
  "name": "@linguolab/e2e",
  "private": true,
  "scripts": {
    "test": "echo 'E2E runs via e2e.yml workflow — skipping in unit CI' && exit 0",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:debug": "playwright test --debug",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "next-auth": "^4.24.14"
  }
}
